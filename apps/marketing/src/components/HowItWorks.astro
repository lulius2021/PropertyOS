---
const steps = [
  {
    number: '01',
    title: 'Registrieren & Objekte anlegen',
    description:
      'Erstellen Sie kostenlos Ihr PropGate-Konto und legen Sie Ihre Immobilien mit allen Details an — Adressen, Einheiten, Größen und mehr.',
  },
  {
    number: '02',
    title: 'Mieter & Verträge erfassen',
    description:
      'Hinterlegen Sie Mieter, Mietverträge und Zahlungsmodalitäten. PropGate erstellt automatisch die Sollstellungen und behält den Überblick.',
  },
  {
    number: '03',
    title: 'Automatisiert verwalten',
    description:
      'Importieren Sie Kontoauszüge per CSV, lassen Sie Zahlungen automatisch matchen und erhalten Sie Mahnvorschläge auf Knopfdruck.',
  },
];
---

<section class="how-section">
  <div class="how-container">
    <!-- Header -->
    <div class="section-header" data-animate="fade">
      <div>
        <p class="section-eyebrow">So funktioniert's</p>
        <h2 class="section-h2">In drei Schritten zur digitalen Verwaltung</h2>
      </div>
      <p class="section-subtitle">
        PropGate ist intuitiv aufgebaut — ohne lange Einarbeitung sofort einsatzbereit.
      </p>
    </div>

    <!-- Steps -->
    <div class="steps-grid" data-stagger-parent>
      {steps.map((step, i) => (
        <div class="step-card" data-stagger>
          <div class="step-number">{step.number}</div>
          {i < steps.length - 1 && <div class="step-connector" aria-hidden="true"></div>}
          <h3 class="step-title">{step.title}</h3>
          <p class="step-description">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .how-section {
    background: var(--bg);
    padding: 5rem 1.5rem;
  }

  .how-container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .section-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: end;
    margin-bottom: 3rem;
  }

  .section-eyebrow {
    font-size: 0.875rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #0066ff;
    margin-bottom: 0.75rem;
  }

  .section-h2 {
    font-size: clamp(2rem, 4vw, 2.75rem);
    font-weight: 700;
    line-height: 1.15;
    letter-spacing: -0.03em;
    color: var(--heading);
    margin-bottom: 1rem;
  }

  .section-subtitle {
    font-size: 1.0625rem;
    line-height: 1.7;
    color: var(--text-muted);
    padding-bottom: 0.25rem;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    position: relative;
  }

  .step-card {
    background: var(--bg-card);
    border: 1px solid var(--border-card);
    border-radius: 12px;
    padding: 2rem 1.75rem;
    position: relative;
    transition: box-shadow 0.2s ease;
  }

  .step-card:not(:last-child) {
    margin-right: 1.25rem;
  }

  .step-card:hover {
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
  }

  .step-number {
    font-size: 2.5rem;
    font-weight: 800;
    color: rgba(0, 102, 255, 0.15);
    line-height: 1;
    margin-bottom: 1.25rem;
    letter-spacing: -0.04em;
  }

  .step-connector {
    display: none;
  }

  .step-title {
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--heading);
    margin-bottom: 0.625rem;
    letter-spacing: -0.01em;
  }

  .step-description {
    font-size: 0.9375rem;
    line-height: 1.65;
    color: var(--text-muted);
  }

  @media (max-width: 900px) {
    .section-header {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }
  }

  @media (max-width: 1024px) {
    .steps-grid {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .step-card:not(:last-child) {
      margin-right: 0;
    }
  }
</style>
